#include<iostream>
using namespace std;

template < class T > void unorderd_set( T var){

typedef struct node{
   struct node* left;
   struct node* right;
   T data;
}node; 

node* set= NULL:

public:
 
 
 into(node* root,T data){
    if(root== NULL){
        root->data=this->data;
    }
    else{

    }
 }
 insert(T data){
    into_tree(set,data);
    // if(set == NULL){
    //     set->data=this->data;
    //     set->next=NULL;
    //     set->right =NULL;
    // }
    // else{
        
    // }
 }
   

};
